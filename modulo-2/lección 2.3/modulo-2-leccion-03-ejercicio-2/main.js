'use strict';

const nameAdalaber = 'Antonia';

if (nameAdalaber === 'Rocío' || nameAdalaber === 'Macarena') {
  alert('Bienvenida, ' + nameAdalaber);
} else {
  alert('Lo siento, pero la usuaria que has introducido no está registrada.');
}