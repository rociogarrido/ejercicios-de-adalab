"use strict";

//  La primera de la pareja con el teclado va a crear un array movies con un listado de 3 películas que le gusten. Será un array de cadenas (strings).

const movies = ["Leviatán", "Gato negro, gato blanco", "Cinema Paradiso"];

// Para terminar este ejercicio, vamos a encapsular todo el código que hemos creado en una función que no toma parámetros y que llamaremos workWithMovies. 

function workWithMovies() {}

// Ejecutamos la función para comprobar que se muestran los mensajes en la consola correspondientes.

console.log(workWithMovies);