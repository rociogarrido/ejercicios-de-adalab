// Haz tres pruebas con distintos números para comprobar que funciona correctamente. 

let number1 = 754;
let number2 = 23;

// Crea una función que reciba como argumentos dos valores y devuelva como valor de retorno la multiplicación de ambos.

function multiply (number1,number2) {
    return number1 * number2;
};

const result = multiply(number1,number2);

// Muestra el resultado en la consola usando console.log().
console.log(result);